export interface YoutubeProperties {
  videoId: string;
  autoplay?: boolean;
  size?: number;
  measurement?: string;
  mute?: boolean;
  loop?: boolean;
  controls?: boolean;
  isBackground?: boolean;
}